import 'package:flutter/material.dart';

class GameBrick extends StatelessWidget {
  const GameBrick({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      height: 25,
      width: 25,
    );
  }
}
